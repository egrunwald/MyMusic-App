<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MyMusic" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- Custom application theme. -->
    <style name="Theme.MyMusicCustom" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/green_400</item>
        <item name="colorPrimaryVariant">@color/green_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
    </style>

    <!-- Custom theme for list items. -->
    <style name="Theme.ListItem" parent="Theme.MaterialComponents">
        <item name="android:background">@color/green_400</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">16sp</item>
    </style>

    <!-- Custom theme for default header button. -->
    <style name="Theme.HeaderButtonDefaultTheme" parent="Theme.MaterialComponents">
        <item name="android:layout_margin">8dp</item>
        <item name="android:padding">8dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:background">@color/green_200</item>
        <item name="android:gravity">center</item>
    </style>

    <!-- Custom theme for currently selected header button. -->
    <style name="Theme.HeaderButtonSelectedTheme" parent="Theme.MaterialComponents">
        <item name="android:layout_margin">8dp</item>
        <item name="android:padding">8dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:background">@color/green_400</item>
        <item name="android:gravity">center</item>
    </style>

    <!-- Custom theme for song info display in the player. -->
    <style name="Theme.SongInfoTheme" parent="Theme.MaterialComponents">
        <item name="android:gravity">center_horizontal</item>
        <item name="android:textSize">12dp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <!-- Custom theme for the player control buttons. -->
    <style name="Theme.PlayerButtonTheme" parent="Theme.MaterialComponents">
        <item name="android:background">@color/green_400</item>
        <item name="android:padding">8dp</item>
    </style>
</resources>